#An example of how to perform database transactions in the simplest forms
from server import db

#db.create_all()	#If initializing the database the first time

#db.session.add(someObject)
#db.session.commit()
#db.session.query(someObject).first()